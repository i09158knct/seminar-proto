<table class="table table-bordered table-hover">
  <thead>
    <tr>
      <th>Title</th>
      <th>User</th>
    </tr>
  </thead>
  <tbody>
    <% @answers.each do |answer| %>
      <tr>
        <td><%= link_to answer.title, [@challenge, answer] %></td>
        <td><%= link_to answer.user.name, answer.user %></td>
      </tr>
    <% end %>
  </tbody>
</table>